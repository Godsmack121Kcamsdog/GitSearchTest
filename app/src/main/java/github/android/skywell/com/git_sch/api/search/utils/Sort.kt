package github.android.skywell.com.git_sch.api.search.utils

object Sort {
    private val SORT = "&sort="

    val STARS = SORT + "stars"
    val FORKS = SORT + "forks"
    val UPDATE = SORT + "updated"
}
